name: Cron Job

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the Streamlit App
        run: curl -sSf -L https://sentiment-analysis-noice-v1.streamlit.app/ > /dev/null
